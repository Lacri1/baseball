apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: baseball-app
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP # 클러스터 내부에서만 접근 가능
